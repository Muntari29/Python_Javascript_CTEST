// squared

// a의 b승을 구해보자

// const number = prompt('두 수를 입력하세요').split(" ");
const number = [2, 3]

const res_number = Math.pow(number[0], number[1])

console.log(res_number)



const number = prompt('두 수를 입력하세요').split(" ");

const res_number = Math.pow(number[0], number[1])

console.log(res_number)
// 8

console.log(Math.pow(parseInt(n[0], 10), parseInt(n[1], 10)));
//  십진수의 형태의 숫자로 데이터 타입을 변환합니다.
// 8 

/*
pow 는 특정숫자의 거듭제곱 값을 계산해주는 함수이며,
sqrt 는 특정숫자의 제곱근 값을 계산해주는 함수입니다. 
*/

/* 
자바스크립트 pow 사용방법
Math.pow(2,3) ==> 8
Math.pow([대상 숫자], [거듭제곱 횟수]);

자바스크립트 sqrt 사용방법
Math.sqrt(4) => 2
Math.sqrt([대상 숫자]);
   */